import styles from './News.module.scss';

function News() {
    return (
        <div className={styles.news}>
            <h1 className={styles.title}>News Page</h1>
        </div>
    );
}

export default News;
